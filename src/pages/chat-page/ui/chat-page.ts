import { Block } from "shared/constructors";

export class ChatPage extends Block {
  constructor() {
    super({});
  }

  override render() {
    return /* HTML */ `<main>Страница чата (находится в разработке)</main>`;
  }
}
